# BGM

ジャンルごとに 1 曲（ループ）を置く。リポジトリにバイナリを置かず、手元のファイルは `local/media.env` の `MANIM_BGM_PATH` か `scripts/add_bg_bgm.sh --bgm` で渡す。

| ファイル | ジャンル |
|---|---|
| `math.mp3` | 数学（図形・無言証明） |
| `math_algebra.mp3` | 数学・代数（任意。未配置なら `math.mp3` を使う） |

ライセンスが明示された CC0 / 商用可の素材のみを入れる。
曲名・作者・許諾は動画の概要欄に転記する。
