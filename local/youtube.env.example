# YouTube Data API の authorized-user JSON（gitignore）。
#   cp local/youtube.env.example は不要。トークンファイルを置く。
#
# YOUTUBE_TOKEN_PATH=local/youtube_token.json
# YOUTUBE_CLIENT_SECRETS=local/youtube_client_secrets.json
#
# スコープ: https://www.googleapis.com/auth/youtube.upload
# 依存: pip install -e '.[youtube]'
