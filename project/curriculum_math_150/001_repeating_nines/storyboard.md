# #1 \(0.999\ldots\) は 1 か

想定尺: 約7分。途中式は残す。待機で時刻に合わせない。文の読み時間は文字数に合わせ、空き待ちでは稼がない。

シリーズの型: 問い → 試行 → 工夫 → 一般化 → まとめ。
「今回のゴール」は学習項目の箇条書きにしない。この問いを解くことに限る。
回を視聴した日付では呼ばない。見出しと本文は「今回」。

## 秒表

| 時刻 | パート |
|---|---|
| 0:00 | 問い |
| 0:40 | 試行 有限桁を縦に引く |
| 2:20 | 試行 \(1/3\) を 3 倍する |
| 3:40 | 工夫 STEP 1 差は \(10^{-n}\)（定義と導出） |
| 5:20 | 工夫 STEP 2 数直線で近づく |
| 6:20 | 工夫 STEP 3 \(10x-x\) |
| 7:40 | 工夫 STEP 4 無限等比級数 |
| 9:00 | 実例 最初から最後まで通す |
| 9:50 | 一般化 無限小数は極限 |
| 10:40 | まとめ |

時刻は目安。途中式と表を残すことを優先する。

---

## 0:00 問い

見出し「\(0.999\ldots\) は 1 か」を中央に出してから上へ固定する。

中央に二つのカードを並べる。左カードの 9 は左から一つずつ点灯する。飛ばさない。

- 左（青）: \(0.999\ldots\)
- 右（緑）: \(1\)

下に問いを出す。答えはすぐ出さない。

- 「この二つの書き方は、同じ数でしょうか。」
- 「それとも、左のほうが、ほんの少し小さいでしょうか。」

ねらい: 試したくなる衝突を残す。結論はまだ書かない。

---

## 0:35 試行 有限桁を 1 から引く

カードを消す。左上に「試行  有限の 9」。

本文を一段ずつ残す。

1. 「まず、9 を有限個だけ書いて、1 から引いてみます。」
2. 「無限はいったん置いて、途中で止めた数だけを見ます。」

縦の引き算を 4 回、一段ずつ残す。表の前に途中式を出す。飛ばさない。

1. \(1.0-0.9=0.1\)
2. \(1.00-0.99=0.01\)
3. \(1.000-0.999=0.001\)
4. \(1.0000-0.9999=0.0001\)

その下に比較表（`MathTable`、列は左揃え）。行を上から順に出す。

| 桁 \(n\) | 有限の 9 | \(1\) との差 |
|---|---|---|
| \(1\) | \(0.9\) | \(1-0.9=0.1\) |
| \(2\) | \(0.99\) | \(1-0.99=0.01\) |
| \(3\) | \(0.999\) | \(1-0.999=0.001\) |
| \(4\) | \(0.9999\) | \(1-0.9999=0.0001\) |

表の下:

- 「差は小さくなります。でも、どこで止めても 0 にはなりません。」
- 「だから『永遠に 1 より小さい』と感じます。」

この直観は消さずに残す。ここでは否定しない。

---

## 2:10 試行 \(1/3\) を 3 倍する

表を消す。左上を「試行  三分の一」に替える。

本文:

- 「一方で、よく知っている分数からも同じ記号が出ます。」

途中式を一段ずつ残す。

1. \(\dfrac{1}{3}=0.333\ldots\)
2. \(3\times\dfrac{1}{3}=1\)
3. 縦に掛ける: \(0.333\ldots\times 3=0.999\ldots\)
4. だから \(0.999\ldots=1\) に見える

下に二つの直観を並べる（幅の広い対比は画面中央）。

| 左（橙） | 右（緑） |
|---|---|
| 有限桁を引く | 三分の一を 3 倍する |
| 差が残る | \(0.999\ldots=1\) に見える |

- 「同じ記号なのに、二つの直観がぶつかります。」
- 「どちらを信じるか、まだ決めません。差の式を最後まで書きます。」

---

## 3:20 工夫 STEP 1 差は \(10^{-n}\)

対比を消す。左上に「STEP 1  差の式」。

本文:

- 「有限桁のほうを、記号で書いてみます。」

途中式（残す）。定義のあと、有限の等比和で \(1-a_n\) を導く。ジャンプしない。

1. \(n\) 個の 9 を並べた数を \(a_n\) と書く
2. \(a_1=0.9,\ a_2=0.99,\ a_3=0.999\)
3. \(a_n=\dfrac{9}{10}+\dfrac{9}{100}+\cdots+\dfrac{9}{10^n}\)
4. \(=\dfrac{9}{10}\cdot\dfrac{1-10^{-n}}{1-1/10}=1-10^{-n}\)
5. よって \(1-a_n=10^{-n}\)
6. 検算: \(n=1\) なら \(10^{-1}=0.1\)。\(n=3\) なら \(10^{-3}=0.001\)

導出が長くなったら、定義を消してから和の変形を出す。残っている本文の上に重ねない。

下:

- 「止めた位置が \(n\) なら、残りはいつも \(10^{-n}\) です。」
- 「『差が残る』は正しい。問題は、\(n\) を止めずに進めたときです。」

定義の式 \(1-a_n=10^{-n}\) は 3 秒以上残す。

---

## 4:40 工夫 STEP 2 数直線で近づく

式を消す。左上に「STEP 2  数直線」。

\(0\) から \(1\) までの棒を画面中央に置く。左が \(a_n\)（青）、右が差（橙）。幅は値に比例させる。点が右端に潰れて読めなくなる配置は使わない。

段階を飛ばさない。

1. \(a_1=0.9\)、橙の幅 \(0.1\)
2. \(a_2=0.99\)、橙の幅 \(0.01\)
3. \(a_3=0.999\)、橙の幅 \(0.001\)
4. 極限では橙が消え、差は \(0\)

各段階の下に差の式を `MathTex` で残す。棒の上にキャプションを重ねない。左に棒、下に式。

下:

- 「点は \(1\) にどんどん近づきます。」
- 「隙間は \(10^{-n}\) で、\(n\) を大きくすると 0 に近づきます。」
- 「高校の言葉では、この極限が 0 だと言います。」

式を残す:

\[
\lim_{n\to\infty}(1-a_n)=\lim_{n\to\infty}10^{-n}=0
\]

この式はあとで使うので、出したあとすぐ消さない（3 秒以上）。

---

## 5:50 工夫 STEP 3 \(10x-x\)

数直線を消す。左上に「STEP 3  10倍して引く」。

本文:

- 「無限に続くほうを、いったん \(x\) と置きます。」

途中式を一段ずつ。上書きして飛ばさない。縦の引き算も残す。

1. \(x=0.999\ldots\)
2. \(10x=9.999\ldots\)
3. 縦に引く: \(9.999\ldots-0.999\ldots=9.000\ldots\)
4. だから \(10x-x=9\)、つまり \(9x=9\)
5. \(x=1\)

下:

- 「9 がどこまでも続くなら、引いたあとに小数部分は残りません。」
- 「有限で止めると残りが出たのは、そこで 9 を切ったからです。」

結論 \(x=1\) は 3 秒以上残す。

---

## 7:10 工夫 STEP 4 無限等比級数

左上を「STEP 4  等比級数」に替える。

本文:

- 「桁ごとにばらして足しても、同じ答えになります。」

途中式:

1. \(0.999\ldots=0.9+0.09+0.009+\cdots\)
2. \(=\dfrac{9}{10}+\dfrac{9}{100}+\dfrac{9}{1000}+\cdots\)
3. \(=\dfrac{9}{10}\left(1+\dfrac{1}{10}+\dfrac{1}{100}+\cdots\right)\)
4. 初項 \(1\)、公比 \(1/10\) の無限等比級数。和を \(S\) と置いて導く
5. \(S=1+1/10+1/100+\cdots\)
6. \(\frac{1}{10}S=1/10+1/100+\cdots\)
7. \(S-\frac{1}{10}S=1\) より \(S=\dfrac{10}{9}\)
8. \(\dfrac{9}{10}\times\dfrac{10}{9}=1\)

下:

- 「ばらして足しても、\(10x-x\) でも、答えは 1 です。」

最後の \(1\) は 3 秒以上残す。

---

## 8:30 実例 最初から最後まで通す

工夫の式を消す。左上に「実例」。

小さな具体例を、最初から最後まで通す。新しい数は出さない。\(0.999\ldots\) だけを辿る。

画面にチェックを順に残す。

1. 有限: \(1-0.9=0.1,\ 1-0.99=0.01,\ 1-0.999=0.001\)
2. 一般: \(1-a_n=10^{-n}\)
3. 極限: \(n\to\infty\) で差は 0
4. 別計算: \(10x-x\) でも等比級数でも \(x=1\)

下:

- 「『差が残る』は、止めたときの話でした。」
- 「止めないときの差は 0 なので、二つの書き方は同じ数です。」

同じ読み方で、\(0.333\ldots=1/3\) も一段だけ辿る。新しい道具は出さない。

1. \(0.333\ldots=0.3+0.03+0.003+\cdots\)
2. \(=\dfrac{3}{10}\left(1+\dfrac{1}{10}+\dfrac{1}{100}+\cdots\right)=\dfrac{3}{10}\times\dfrac{10}{9}=\dfrac{1}{3}\)

---

## 9:30 一般化 無限小数は極限

実例を消す。左上に「一般化」。

本文を一段ずつ残す。

1. 「今やったことは、無限小数を極限として読む、ということでした。」
2. 「\(0.999\ldots\) は、『9 を \(n\) 個書いた数』の、\(n\) を限りなく進めた先です。」
3. 「その先では差 \(10^{-n}\) が 0 になるので、\(1\) と同じ実数です。」
4. 「書き方が二つあっても、指している数は一つ、ということがあります。」

式を残す（すぐ消さない）:

\[
0.999\ldots\ :=\ \lim_{n\to\infty}a_n=\lim_{n\to\infty}(1-10^{-n})=1
\]

\(\varepsilon\)–\(\delta\) は出さない。高校の極限の言葉で閉じる。

---

## 10:20 まとめ

一般化を消す。左上に「まとめ」。

持ち帰れる型だけ。次回必見にはしない。

1. 有限個の 9 を 1 から引くと、差 \(10^{-n}\) が残る
2. 9 を止めずに進めると、その差は 0 に収束する
3. だから \(0.999\ldots\) と \(1\) は同じ数
4. 無限小数は、「どこまでも続く桁」の極限として読む

関連する別の問い（依存を作らない紹介）:

- 「循環小数が分数になる話も、同じ読み方で閉じます。」

「先に別の回を見ること」とは書かない。
